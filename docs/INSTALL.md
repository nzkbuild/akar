# AKAR Install Guide

## A. Installation

### Requirements

- Rust 1.70+ and Cargo
- Windows 10/11 (primary target); macOS and Linux supported
- No other runtime dependencies — AKAR is a single self-contained binary

### Build from source

```powershell
git clone https://github.com/nzkbuild/akar.git
cd akar
cargo build --release
```

The binary is at `target\release\akar.exe` (Windows) or `target/release/akar` (macOS/Linux).

### Add to PATH (Windows)

```powershell
# Copy to a directory already on your PATH, e.g.:
copy target\release\akar.exe C:\Users\<you>\bin\akar.exe

# Or add the release dir to PATH for this session:
$env:PATH += ";$PWD\target\release"
```

### Add to PATH (macOS/Linux)

```bash
cp target/release/akar ~/.local/bin/akar
# or
sudo cp target/release/akar /usr/local/bin/akar
```

### Verify install

```
akar --version
```

Expected: `akar <current version>` (e.g. `akar 0.22.0`). Run `akar --version` to confirm; the version always matches `Cargo.toml`.

### Update

Pull the latest source and rebuild:

```powershell
git pull
cargo build --release
```

Replace the binary in your PATH with the new build.

### Uninstall

Delete the binary from your PATH. No registry entries, no daemons, no background processes. AKAR leaves no system-level traces.

---

## B. Runtime Layout

AKAR uses two directories. Neither is touched until you run `akar init` or `akar bootstrap`.

### Project-local: `.akar/`

Created inside your project root. Owns all project-scoped runtime state.

```
.akar/
  PROJECT_DNA.md     — project identity, stack, constraints
  STATE.md           — current goal, done, next, blockers
  DECISIONS.md       — important decisions and their rationale
  LESSONS.md         — reusable lessons observed during missions
  KNOWN_BUGS.md      — bug patterns and prevention notes
  TEST_DEBT.md       — weak/stale/flaky test records
  DESIGN_DNA.md      — UI/product design rules for this project
  MODEL_PROFILE.md   — active model and gateway profile
  VERIFY_RECIPE.md   — project-specific verification commands
  EVENT_LOG.jsonl    — append-only operational telemetry (gitignored)
  SKILL_INVENTORY.md — generated by akar skills (gitignored)
```

The `.md` template files are committed to git as your project's starter state.
`EVENT_LOG.jsonl` and generated files are gitignored by default.

### Global: `~/.claude/akar/`

Created once per machine. Owns global AKAR config and cross-project state.

```
~/.claude/akar/
  (future: global lessons, model cache, cross-project telemetry)
```

AKAR does NOT modify `~/.claude/settings.json` or any other Claude Code config unless you explicitly wire hooks (see Section G).

### State ownership rules

- `.akar/` belongs to the project. Commit the `.md` files. Gitignore the rest.
- `~/.claude/akar/` belongs to the machine. Never commit it.
- AKAR never reads or writes outside these two directories during normal operation.

---

## C. Configuration

### Config discovery

AKAR discovers its config from the current working directory at runtime. There is no global config file to edit for normal use.

```
project_root  = cwd (canonicalized)
akar_dir      = project_root/.akar/
global_dir    = ~/.claude/akar/
project_name  = basename of project_root
```

### Configuration precedence

```
1. Runtime flags (--used, --limit, --prompt, --fix, --claude, --skip)
2. Project memory files (.akar/*.md)
3. Global AKAR dir (~/.claude/akar/)
4. Compiled-in defaults
```

No env vars are required. No `.env` file. No TOML/JSON config file.

### Verify configuration

```
akar status
```

Shows: runtime version, project name, doctor state, bootstrap state, telemetry count, postmortem outcome, skill conflicts, request pressure mode.

### Migrate between versions

AKAR memory files are plain Markdown. They are forward-compatible: new versions add new template sections but never remove existing ones. To migrate:

1. Pull the new binary.
2. Run `akar doctor` — it will report any missing files.
3. Run `akar doctor --fix` — it copies any new templates without overwriting your existing content.

### Version compatibility

| Binary | Template format | Notes |
|---|---|---|
| v0.1.x | v1 | Initial layout, 9 templates |
| v0.2.x | v1 | Same layout, additive changes only |
| v0.5.x | v1 | PreToolUse hook templates added; additive only |
| v0.6.x | v1 | Real diff measurement (`postmortem --diff`); additive only |
| v0.13.x–v0.20.x | v1 | Loop governor, NEXT_RUN compiler, request `--check` validator; additive only |
| v0.21.x–v0.22.x | v1 | Audit + honest-edges cleanup; no template format change |

AKAR memory files are plain Markdown and forward-compatible: new versions add template sections but never remove existing ones. The template format has not changed since v1.

---

## External repo setup

To use AKAR in advisory mode on a repo that is not AKAR itself:

1. Ensure `akar` is on PATH (`akar --version` works from any directory).
2. In the target repo:
   - `akar init` — bootstrap `.akar/` + doctor + next-steps guide
   - `akar hooks --install` — write the embedded PreToolUse hook templates to `.akar/hooks/` (works without the AKAR source tree; never edits `~/.claude/settings.json`)
   - manually wire Claude Code PreToolUse if not already wired globally (run `akar hooks` for the settings.json example)
   - `akar hooks --check` — verify templates (source-tree, project `.akar/hooks/`, or embedded)
   - `akar doctor` — read-only dogfood-readiness check

`akar doctor` and `akar hooks --check` no longer FAIL on a fresh external repo just because the AKAR source-tree templates are absent — the embedded fallback covers it. To activate real PreToolUse enforcement, install the templates and register the hook in `~/.claude/settings.json` manually (AKAR never does this for you).

### Recommended dogfood command

Once bootstrapped, the recommended shape for starting a session is to pass the actual task straight to `akar request` so the compiled `.akar/NEXT_RUN.md` prompt is self-describing instead of generic:

```
akar request "fix one small failing test"
```

Hand the resulting `.akar/NEXT_RUN.md` to Claude as-is. Run `akar request --check` afterward if you want to validate the prompt before handing it off.

---

## Troubleshooting

| Command | Purpose |
|---|---|
| `akar doctor` | Read-only health check |
| `akar doctor --fix` | Safe reversible fixes |
| `akar status` | Full runtime health at a glance |
| `akar init` | Re-run onboarding if something is broken |
